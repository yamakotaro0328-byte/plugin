<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.yamakotaro</groupId>
    <artifactId>velodicord-bridge</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>Velodicord-Bridge</name>
    <description>Paper-side companion for Velodicord: reports player deaths/advancements and
        /pos to the Velocity proxy over a WebSocket, with no Discord bot of its own (Velodicord's
        single bot handles all Discord posting). Replaces the Fabric-only Fabdicord mod so a
        Paper/Spigot backend server doesn't need Fabric to talk to Velodicord.</description>

    <!-- Independent Maven project (its own pom.xml), built in its own CI job so a build
         failure here never blocks or reflects on any of the other plugins. -->

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>[26.2.build,)</version>
            <scope>provided</scope>
        </dependency>
        <!-- Lightweight embedded WebSocket server; this plugin listens, the Velocity-side
             Velodicord proxy connects out to it (same direction as Velodicord's existing
             WebSocketClient / WebSocketPluginMessageManager transport). Not proxy/server
             provided, so it's shaded into this plugin's jar (unrelocated - no other plugin
             on this server bundles it, and shade-plugin 3.5.1's relocation rewrite can't
             parse Paper's Java 25 (class file 69) bytecode in our own compiled classes). -->
        <dependency>
            <groupId>org.java-websocket</groupId>
            <artifactId>Java-WebSocket</artifactId>
            <version>1.5.7</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <defaultGoal>package</defaultGoal>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>25</release>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
