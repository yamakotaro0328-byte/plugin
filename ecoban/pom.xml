<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.yamakotaro</groupId>
    <artifactId>ecoban</artifactId>
    <version>1.0.0</version>
    <packaging>jar</packaging>

    <name>EcoBan</name>
    <description>Ban/mute/kick/warn management for Paper, with SQLite or network-shared MySQL
        storage, migration from AdvancedBan and LiteBans, and a built-in web dashboard. Pairs with
        ecoban-velocity for network-wide ban enforcement across a Velocity proxy.</description>

    <!-- Independent Maven project (its own pom.xml), built in its own CI job. Depends on
         ecoban-core (a sibling module, installed to the local Maven repository first in CI -
         see build-ecoban in .github/workflows/build.yml) for all the platform-agnostic
         punishment logic, storage, migration, and the web dashboard; shaded into this module's
         own jar since ecoban-core isn't a server-provided API. -->

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
    </properties>

    <repositories>
        <repository>
            <id>papermc</id>
            <url>https://repo.papermc.io/repository/maven-public/</url>
        </repository>
    </repositories>

    <dependencies>
        <dependency>
            <groupId>io.papermc.paper</groupId>
            <artifactId>paper-api</artifactId>
            <version>[26.2.build,)</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>com.yamakotaro</groupId>
            <artifactId>ecoban-core</artifactId>
            <version>1.0.0</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <defaultGoal>package</defaultGoal>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
            </resource>
        </resources>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>25</release>
                </configuration>
            </plugin>
            <!-- ecoban-core and its own dependencies (sqlite-jdbc, mysql-connector-j, gson)
                 aren't server-provided, so they need to be bundled into this plugin's jar. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
